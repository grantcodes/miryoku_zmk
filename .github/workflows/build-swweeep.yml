# Copyright 2023 Manna Harbour
# https://github.com/manna-harbour/miryoku

name: 'Build Swweeep'
on: workflow_dispatch
jobs:
  build:
    uses: ./.github/workflows/main.yml
    secrets: inherit
    with:
      board: '["nice_nano_v2"]'
      shield: '["swweeeep_left","swweeeep_right"]'
      custom_config: '["#define MIRYOKU_CLIPBOARD=WIN"]'
